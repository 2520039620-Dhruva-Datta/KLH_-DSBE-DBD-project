import {Link} from 'react-router';
import API from '../../data/api.js';
import useApi from '../../hooks/useApi.js';
import {useAuth} from '../../context/AuthContext.jsx';
import {ApiState,Badge,ButtonLink,Card,EmptyState,PageHead,Progress,Stats,Table,Timeline} from '../../components/ui/index.jsx';
import {Donut} from '../../components/charts/index.jsx';
import {statusColor} from '../../lib/status.js';
import {fmtDate} from '../../lib/format.js';
export default function Dashboard(){const {user}=useAuth();const state=useApi(async()=>{const scope={user_id:user.id};const [kpis,applications,status,grievances]=await Promise.all([API.analytics.kpis(scope),API.applications.list({limit:6}),API.analytics.byStatus(scope),API.grievances.list({limit:4})]);const latest=applications.rows[0],logs=latest?await API.applications.logs(latest.id):[];return {kpis,applications,status,grievances,latest,logs};},[user.id]);const d=state.data;return <><PageHead title={'Welcome, '+user.full_name.split(' ')[0]} lead="A clear view of your requests and their next steps."><ButtonLink to="/citizen/apply" icon="filePlus">Apply for a service</ButtonLink></PageHead><ApiState state={state}>{d&&<><Stats items={[{label:'Applications',value:d.kpis.total},{label:'Open now',value:d.kpis.pending,icon:'clock'},{label:'Approved',value:d.kpis.approved,icon:'checkCircle',tone:'ok'},{label:'Needs your attention',value:d.status.find(s=>s.key==='NEEDS_INFO')?.value||0,icon:'alert',tone:'warn'}]}/><div className="dash-grid"><div className="col-4"><Card title="Quick actions"><div className="quick-grid">{[['/citizen/apply','New application','Choose a service'],['/citizen/applications','My applications','Review your history'],['/citizen/grievances','File a grievance','Ask for help'],['/profile','My profile','Keep details up to date']].map(([to,label,sub])=><Link className="quick" key={to} to={to}><b>{label}</b><span>{sub}</span></Link>)}</div></Card></div><div className="col-8"><Card title="Your most recent application" subtitle={d.latest?.ref}>{d.latest?<><h3 className="mb-4">{d.latest.service.name}</h3><Progress application={d.latest}/><Timeline logs={d.logs.slice(-3)}/><div className="mt-5"><ButtonLink to={'/citizen/applications/'+d.latest.id} variant="outline">View full application</ButtonLink></div></>:<EmptyState title="Your first application starts here" message="Choose a service to begin. You’ll get a reference number when you submit."><ButtonLink to="/citizen/apply">Apply for a service</ButtonLink></EmptyState>}</Card></div><div className="col-8"><Card title="Recent applications" flush><Table caption="Recent applications" rows={d.applications.rows} columns={[{key:'ref',label:'Reference',render:a=><Link to={'/citizen/applications/'+a.id}>{a.ref}</Link>},{key:'service',label:'Service',render:a=>a.service.name},{key:'department',label:'Department',render:a=>a.department.code},{key:'submitted_at',label:'Submitted',render:a=>fmtDate(a.submitted_at)},{key:'status',label:'Status',render:a=><Badge status={a.status}/>}]}/></Card></div><div className="col-4 stack"><Card title="Where your applications stand"><Donut slices={d.status.map(s=>({...s,color:statusColor(s.key)}))}/></Card><Card title="Recent grievances" actions={<Link to="/citizen/grievances">Manage</Link>}>{d.grievances.rows.length?d.grievances.rows.map(g=><div className="grievance-summary" key={g.id}><Badge status={g.status}/><p className="mt-2">{g.subject}</p><small className="muted">{g.ref}</small></div>):<p className="muted">No grievances filed. If a service needs attention, you can raise it here.</p>}</Card></div></div></>}</ApiState></>;}
