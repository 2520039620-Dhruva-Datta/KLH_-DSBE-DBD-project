import {useState} from 'react';
import {Link,Navigate,useSearchParams} from 'react-router';
import {useAuth,homeFor} from '../../context/AuthContext.jsx';
import {Alert,Button,Field,PasswordInput} from '../../components/ui/index.jsx';
import {safeNext} from '../../lib/format.js';
const cities=['Visakhapatnam','Vijayawada','Guntur','Tirupati','Kakinada','Rajahmundry','Nellore','Kurnool','Anantapur','Hyderabad','Warangal','Khammam'];
export default function Register(){const auth=useAuth(),[params]=useSearchParams(),[form,setForm]=useState({full_name:'',email:'',phone:'',city:'',address:'',id_last4:'',password:'',confirm:''}),[busy,setBusy]=useState(false),[error,setError]=useState('');const bind=key=>({value:form[key],onChange:e=>setForm({...form,[key]:e.target.value})});if(auth.user)return <Navigate replace to={safeNext(params.get('next'),homeFor(auth.user.role))}/>;async function submit(e){e.preventDefault();setError('');if(form.password!==form.confirm){setError('The two passwords do not match.');return;}setBusy(true);try{const {confirm,...data}=form;await auth.register(data);}catch(e){setError(e.message);}finally{setBusy(false);}}return <div className="auth"><aside className="auth-aside"><div><h2>Register once. Apply to every department.</h2><p>Your profile, applications and documents come together in one place.</p><ul className="auth-points"><li>Your details prefill application forms.</li><li>Follow every request from submission to decision.</li><li>Keep an accessible record of the officer’s remarks.</li></ul></div><p>Government Services Portal — Academic Prototype</p></aside><section className="auth-main"><div className="auth-card register-card"><h1>Create a citizen account</h1><p className="lead">Use sample details for this academic demonstration.</p>{error&&<Alert type="danger">{error}</Alert>}<form onSubmit={submit}><Field label="Full name" required minLength={3} autoComplete="name" {...bind('full_name')}/><div className="grid-2"><Field label="Email address" type="email" required autoComplete="email" {...bind('email')}/><Field label="Mobile number" type="tel" required pattern="[6-9][0-9]{9}" maxLength={10} hint="A 10-digit mobile number" {...bind('phone')}/><Field label="City / District" as="select" required {...bind('city')}><option value="">Select a city</option>{cities.map(c=><option key={c}>{c}</option>)}</Field><Field label="ID last 4 digits" inputMode="numeric" pattern="[0-9]{4}" maxLength={4} hint="Optional. Only four digits are stored." {...bind('id_last4')}/></div><Field label="Residential address" as="textarea" required minLength={8} autoComplete="street-address" {...bind('address')}/><PasswordInput required minLength={8} autoComplete="new-password" showMeter {...bind('password')}/><Field label="Confirm password" type="password" required minLength={8} autoComplete="new-password" {...bind('confirm')}/><label className="check mb-5"><input type="checkbox" required/><span>I confirm these details are correct and consent to their use for processing my applications.</span></label><Button type="submit" busy={busy} className="btn-block">Create account</Button></form><p className="mt-5">Already registered? <Link to="/login">Sign in</Link></p></div></section></div>;}
