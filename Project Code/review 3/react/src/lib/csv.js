export function toCSV(rows,headers) { const quote=v=>{let s=String(v??'');if(/^[=+@\-\t\r]/.test(s))s="'"+s;return '"'+s.replaceAll('"','""')+'"';};return [headers.map(h=>quote(h.label)).join(','),...rows.map(r=>headers.map(h=>quote(typeof h.key==='function'?h.key(r):r[h.key])).join(','))].join('\r\n'); }
export function downloadBlob(blob,filename) {const url=URL.createObjectURL(blob),link=document.createElement('a');link.href=url;link.download=filename;document.body.append(link);link.click();link.remove();setTimeout(()=>URL.revokeObjectURL(url),10000);}
export const downloadCSV=(filename,rows,headers)=>downloadBlob(new Blob(['\uFEFF'+toCSV(rows,headers)],{type:'text/csv;charset=utf-8'}),filename);
export const applicationHeaders=[{key:'ref',label:'Reference'},{key:r=>r.citizen?.full_name,label:'Applicant'},{key:r=>r.service?.name,label:'Service'},{key:r=>r.department?.name,label:'Department'},{key:'subject',label:'Subject'},{key:'priority',label:'Priority'},{key:'submitted_at',label:'Submitted'},{key:'statusLabel',label:'Status'},{key:r=>r.officer?.full_name,label:'Officer'},{key:'sla_days',label:'SLA days'},{key:'isOverdue',label:'Overdue'},{key:'decided_at',label:'Decided'},{key:'processingDays',label:'Processing days'},{key:'remarks',label:'Remarks'}];
